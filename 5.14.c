#include <stdio.h>
#include <math.h>
#include <string.h>

int main()
{
    char a[100], b[100], c[100];
    scanf("%[^\n]", a);
    for (int i; a[i]; i++){
        if (a[i]!=' '){
            b[i]=a[i];
        }
        else
            
    }
    return 0;
}